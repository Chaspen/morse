/* 
this is likely a genuinely awful way to do this but I was having
alot of trouble w/ else statements while trying to impliment
this earler on in the development of this project 
*/

function findLetter() {
    if (letter == "a") {
        setTimeout(function(){ short(); }, 0);
        setTimeout(function(){ long(); }, 150);
    }

    if (letter == "b") {
        setTimeout(function(){ long(); }, 0);
        setTimeout(function(){ short(); }, 150);
        setTimeout(function(){ short(); }, 300);
        setTimeout(function(){ short(); }, 450);

    }
    
    if (letter == "c") {
        setTimeout(function(){ long(); }, 0);
        setTimeout(function(){ short(); }, 150);
        setTimeout(function(){ long(); }, 300);
        setTimeout(function(){ short(); }, 450);
    }

    if (letter == "d") {
        setTimeout(function(){ long(); }, 0);
        setTimeout(function(){ short(); }, 150);
        setTimeout(function(){ short(); }, 300);
    }
    

    if (letter == "e") {
        setTimeout(function(){ short(); }, 0);
    }

    if (letter == "f") {
        setTimeout(function(){ short(); }, 0);
        setTimeout(function(){ short(); }, 150);
        setTimeout(function(){ long(); }, 300);
        setTimeout(function(){ short(); }, 450);
    }

    if (letter == "g") {
        setTimeout(function(){ long(); }, 0);
        setTimeout(function(){ long(); }, 150);
        setTimeout(function(){ short(); }, 300);
    }

    if (letter == "h") {
        setTimeout(function(){ short(); }, 0);
        setTimeout(function(){ short(); }, 150);
        setTimeout(function(){ short(); }, 300);
        setTimeout(function(){ short(); }, 450);
    }

    if (letter == "i") {
        setTimeout(function(){ short(); }, 0);
        setTimeout(function(){ short(); }, 150);
    }

    if (letter == "j") {
        setTimeout(function(){ short(); }, 0);
        setTimeout(function(){ long(); }, 150);
        setTimeout(function(){ long(); }, 300);
        setTimeout(function(){ long(); }, 450);
    }

    if (letter == "k") {
        setTimeout(function(){ long(); }, 0);
        setTimeout(function(){ short(); }, 150);
        setTimeout(function(){ long(); }, 450);
    }

    if (letter == "l") {
        setTimeout(function(){ short(); }, 0);
        setTimeout(function(){ long(); }, 150);
        setTimeout(function(){ short(); }, 300);
        setTimeout(function(){ short(); }, 450);
    }

    if (letter == "m") {
        setTimeout(function(){ long(); }, 0);
        setTimeout(function(){ long(); }, 150);
    }

    if (letter == "n") {
        setTimeout(function(){ long(); }, 0);
        setTimeout(function(){ short(); }, 150);
    }

    if (letter == "o") {
        setTimeout(function(){ long(); }, 0);
        setTimeout(function(){ long(); }, 150);
        setTimeout(function(){ long(); }, 300);
    }

    if (letter == "p") {
        setTimeout(function(){ short(); }, 0);
        setTimeout(function(){ long(); }, 150);
        setTimeout(function(){ long(); }, 300);
        setTimeout(function(){ short(); }, 450);
    }

    if (letter == "q") {
        setTimeout(function(){ long(); }, 0);
        setTimeout(function(){ long(); }, 150);
        setTimeout(function(){ short(); }, 300);
        setTimeout(function(){ long(); }, 450);
    }

    if (letter == "r") {
        setTimeout(function(){ short(); }, 0);
        setTimeout(function(){ long(); }, 150);
        setTimeout(function(){ short(); }, 300);
  
    }

    if (letter == "s") {
        setTimeout(function(){ short(); }, 0);
        setTimeout(function(){ short(); }, 150);
        setTimeout(function(){ short(); }, 300);
  
    }

    if (letter == "t") {
        setTimeout(function(){ short(); }, 0);
    }

    if (letter == "u") {
        setTimeout(function(){ short(); }, 0);
        setTimeout(function(){ short(); }, 150);
        setTimeout(function(){ long(); }, 300);

    }

    if (letter == "v") {
        setTimeout(function(){ short(); }, 0);
        setTimeout(function(){ short(); }, 150);
        setTimeout(function(){ short(); }, 300);
        setTimeout(function(){ long(); }, 450);
    }

    if (letter == "w") {
        setTimeout(function(){ short(); }, 0);
        setTimeout(function(){ long(); }, 150);
        setTimeout(function(){ long(); }, 300);

    }

    if (letter == "x") {
        setTimeout(function(){ long(); }, 0);
        setTimeout(function(){ short(); }, 150);
        setTimeout(function(){ short(); }, 300);
        setTimeout(function(){ long(); }, 450);
    }

    if (letter == "y") {
        setTimeout(function(){ long(); }, 0);
        setTimeout(function(){ short(); }, 150);
        setTimeout(function(){ long(); }, 300);
        setTimeout(function(){ long(); }, 450);
    }

    if (letter == "z") {
        setTimeout(function(){ long(); }, 0);
        setTimeout(function(){ long(); }, 150);
        setTimeout(function(){ short(); }, 300);
        setTimeout(function(){ short(); }, 450);
    }

}